# STATUS: vivo

> Last updated: 2026-09-17

## Current State
**T001–T023 complete and verified.** A `[user]` profile section (T023, D019) now lives in `vivo.toml` (settings-pane row; env `USER_NAME`/`USER_LOCATION`/`USER_TIMEZONE`/`USER_UNITS`): name, place-name location, IANA timezone, units (metric/imperial) — `get_time()` answers in the profile's timezone (`tzdata` added to the image), `weather()` defaults to the profile's location (geocoded via open-meteo's keyless API, cached) and reports in the preferred units, and the profile is appended to the agent's system prompt. The TTS engine (T022, D018) is now **LuxTTS** (zipvoice, CPU torch + onnxruntime, ~0.2× realtime, **48 kHz**, voice *cloning* from a reference clip) — swapped from kokoro-onnx 82M (~1.08× realtime, 54 fixed voices, 24 kHz) so vivo can speak in the user's own voice. Voices are now 3–30 s reference WAV clips under `data/voices/<slug>.wav` (a bundled `voices/default.wav` is seeded on first start); each clip is encoded once (whisper-tiny + feature extraction) on first use or by a background `prime()` after upload. Backend adds `POST /api/voice` (upload → store → activate → broadcast), `GET /api/voices/{name}` (preview), `DELETE /api/voices/{name}` (active protected). The settings-pane voice row gains **upload / record / preview / delete** (browser re-encodes mic capture to 16-bit mono WAV); playback + test fixtures + bench all move 24 → 48 kHz. `models.py` pre-fetches the two HF repos into `HF_HOME=/models/hf`; `sentence_pause_s` now appends real trailing silence per chunk. The container runs the LuxTTS pipeline (**131/131** suite, ~78 s); it is a full agent (D010, T012) with **named conversation sessions** (D017, T021), a decoupled LLM/TTS pipeline (D011, T014), **thinking model** (D012, T015) with **spoken fillers**, **clause-level TTS** (D016, T020), **auto barge-in** (D013, T017), two-stage VAD (D013, T016), and **`vivo.toml`-driven settings** editable from the UI (D014/D015, T018/T019). `vivo.toml` carries the user's live choices (`max_tokens` 608, `vad.min_silence_ms` 2900) — tests no longer pin shipped defaults. T023 is **uncommitted** in the working tree on top of `8ad753e`.

## In Progress
- <none> — T023 completed (see Recently Completed).

## Recently Completed
- **T023 / D019 — User profile in vivo.toml (name, location, timezone, units).** New `[user]` section (settings-pane row, env `USER_NAME`/`USER_LOCATION`/`USER_TIMEZONE`/`USER_UNITS`, `apply: "now"`, write-back like every other key): name, place-name location (e.g. "Bristol, UK"), IANA timezone, `units` (metric/imperial). `Agent` appends an "About the user" block to the system prompt after the persona (rebuilt on `apply_config`); `get_time()` resolves the profile's zone with `zoneinfo` (`tzdata` added to the image — `python:3.11-slim` ships no zoneinfo data) and reports `in {label}`, falling back to container-local on empty/unknown zone; `weather()` takes an optional place name — defaulting to the profile's location, resolved through open-meteo's keyless geocoding API (in-process cache per place) — and reports in the preferred units (imperial → °F/mph). Nothing set ⇒ no prompt block, local time, weather requires an explicit place name (clear error), metric. **Verified:** full suite **142/142** in the app image (140 via `docker compose run`, + the 2 live-WS pipeline tests via `-e WS_URL=ws://vivo:8000/ws`); new tests: tz-aware get_time (valid + invalid zone), geocoded weather (default location, named location, imperial, no-location/unknown-place errors, geocode caching, live Berlin), profile-in-prompt + config wiring; live: image rebuilt with `tzdata`, service recreated, `GET /api/config` shows the `user` section + defaults, settings pane renders the row.
- **T022 / D018 — TTS swap to LuxTTS voice cloning (reference-clip voices, 48 kHz).** `app/tts.py` rewritten around `zipvoice.luxvoice.LuxTTS` (CPU torch + onnxruntime, 48 kHz): voices are 3–30 s reference WAV clips under `data/voices/<slug>.wav` (mono, resampled to 24 kHz int16 on store), each encoded once (whisper-tiny + feature extraction, `encode_prompt`) on first use or by a background `prime()` after upload; `sentence_pause_s` now appends real trailing silence per chunk. **Short-text guard** (gotcha): LuxTTS's `generate_cpu` hard-multiplies speed ×1.3, so its flow-matching length model budgets *negative* new frames for ≲3–4-word sentences → the vocoder's kernel-7 conv throws and would kill the per-sentence TTS worker (→ whole reply silent). `TTS.synthesize` now predicts the frame budget via the ONNX text encoder (`_new_frames`) and lowers per-utterance speed (`_pick_speed`, floor 0.6) until ≥ 24 new frames, and never raises (empty → worker skips). `app/models.py` pre-fetches `YatharthS/LuxTTS` + `openai/whisper-tiny` into `HF_HOME=/models/hf` and seeds bundled `voices/default.wav`; `app/main.py` adds `POST /api/voice` (upload→store→activate→broadcast→background prime), `GET /api/voices/{name}` (preview), `DELETE /api/voices/{name}` (active→409, missing→404); `static/` voice row gains upload/record/preview/delete (browser re-encodes mic to 16-bit mono WAV); playback + fixtures + bench move 24→48 kHz; `requirements.txt`/`Dockerfile` install CPU torch first then git `LinaCodec`+`LuxTTS` (drops kokoro-onnx); `models/kokoro` removed. **Verified:** full suite **131/131** in the app image (single red run was a transient external Open-Meteo `503` in `test_weather`, green on re-run); live smoke — upload→activate (`duration_s`), preview (RIFF/WAV), active-delete `409`, missing-delete `404`, voice restored to `default`; bench RTF 0.20–0.21 vs kokoro ~1.0, clone quality user-confirmed.
- T021 named conversation sessions (D017): `SessionStore` (new, `app/conversation.py`) — many named sessions, one *active*; each session is a `Conversation` persisted at `data/sessions/<id>.json` + index `data/sessions.json` (`{active, sessions:{id:{created,last_used,turns}}}`), turn counts kept fresh via a new `Conversation.on_change` callback (fires under the conversation lock after each save; no lock inversion). Ids `YYYYMMDD-HHMMSS` + `-N` collision suffixes; **retired ids never reused** (in-memory `_retired` set, so a same-second recreation can't rebind other tabs' pins to empty memory). First start: legacy `data/conversation.json` imported + removed, orphan `sessions/*.json` adopted, corrupt index/session file ⇒ fresh start with a warning. WS (`app/pipeline.py`): `/ws?session=<id>` pins the connection (unknown → active); **first frame** `{"type":"session","id":…}` before the existing `config` frame (server is authoritative — covers the fallback); `{"type":"session"}` creates / `{"type":"session","id":…}` switches, both also set the store's active (plain connections follow where the user last left off); unknown id → `error`; a switch is captured per utterance so an in-flight reply keeps its conversation. REST (`app/main.py`): `GET/POST /api/sessions`, `POST /api/sessions/{id}/activate`, `DELETE /api/sessions/{id}` (404 unknown; deleting the last creates a fresh active). UI (`static/`): footer session dropdown (ids rendered "Sep 17 10:24", active marked "(active)") + **new** button; each tab remembers its session in `localStorage` and reconnects with `?session=`; the server's `session` frame overrides stale state. `[memory]` limits hot-apply per session (`set_limits`, `live` badge). Test fixtures migrated to real `SessionStore` (`test_tts_queue`, `test_thinking_fillers` — dropped `FakeConversation`, `test_config_api`); the test `_CmdWS.receive()` fake now yields to the event loop (`await asyncio.sleep(0)`) + drain after `serve_session` — in single-loop tests, `VoiceSession.send`'s `run_coroutine_threadsafe` tasks only run once the loop yields (true in production: `ws.receive()` awaits the network); production code untouched. **22 new offline tests** (`tests/test_sessions.py`: store CRUD + last-delete + no-reuse, persistence round-trip, on_change index + corrupt-callback tolerance, corrupt index/session ⇒ fresh, orphan adoption, legacy migration, `Conversation` callback, `VoiceSession` bind/pin/switch/fallback, `serve_session` handshake + commands + `?session=` pin, `/api/sessions` CRUD + 404s) → full suite **128/128 in 82.91 s** (live WS tests pass against the rebuilt server). Live: image rebuilt + service `--force-recreate` → first start created `data/sessions.json` + `data/sessions/20260917-101749.json` (one active session; no legacy file to migrate), `/health` OK.
- T020 sub-sentence TTS streaming — clause chunking (D016): `SentenceChunker` now splits an in-progress sentence at clause punctuation (comma/semicolon/colon) once the buffer exceeds `[voice] clause_max_chars` (env `CLAUSE_MAX_CHARS`, default **40**, `0` = off, apply `next`), so TTS starts on the first ~40-char clause instead of waiting for the sentence ending; the split takes the **last** boundary in the buffer (a boundary nearer than half the threshold waits — the 90-char hard split stays the no-punctuation backstop). Spoken text unchanged — only chunk boundaries move. Schema/config/compose/toml/README all carry the knob; the settings pane shows it with a `next session` badge. **11 new tests** (`tests/test_clause_chunking.py`: 10 unit + 1 pipeline — first clause reaches TTS *before* the sentence completes) → full suite **106/106 in 102 s**. Fresh thinking-on bench (repeats 2, reply/long/barge; host LLM shared with the session so thinking 2.8–24.6 s is contention-noisy): TTS-side first audio (`llm_first_token`→first `tts_*_sent`) **3.7–8.6 s → 4.5–5.7 s**; first-chunk synth **3.4–6.9 s → 2.5–5.1 s**; server `first_audio` reply **20.8/30.0 → 10.3/8.8 s**, long **16.7/14.0 → 11.2/13.5 s**; worst inter-chunk gap **2.6 → 2.0 s**; barge-in unchanged (0 stale bytes). Harness fix: the endpointing tail silence in `tests/test_pipeline.py` + `bench.py` is now derived from the service's effective `vad.min_silence_ms + reopen_ms` (+0.5 s) — the fixed 1–1.5 s tail had stopped endpointing the fixtures after the user raised `min_silence_ms` to 2900 (invalidated the first baseline: 0 audio frames). Config tests de-pinned from shipped defaults (`vivo.toml` is user-editable since T019).
- T019 settings pane (D015): gear button opens a `<dialog>` rendered entirely from `app/config_schema.py` `SCHEMA` (9 sections / 36 keys — type/label/tooltip/apply semantics) — sliders with live readout, dropdowns (kokoro voice list from the loaded engine: 54 voices), checkboxes, phrases textarea; each row shows a `live` / `next session` / `restart` apply badge. `GET /api/config` → `{values, schema, voices, path}`; `POST /api/config` → validate (400 + per-key errors) → coerce → write back to the host `vivo.toml` (direct write — the file mount can't `os.replace` across filesystems) → `refresh()` → hot-apply `now` keys (`pipeline.apply_config`: LLM, persona, filler phrases, TTS voice/speed/pause, STT language/beam, memory, agent + exec/web limits incl. the `shell.MAX_TIMEOUT` snapshot) → `broadcast_config()` re-sends the `barge_in` frame to every open WS session. Compose mount now read-write; loader tolerates a corrupt file (warn + defaults). 12 new tests (`tests/test_config_api.py`) → **95/95 in 79.83 s**; live: `GET` (54 voices), `POST tts_speed=1.25` → 200 + host file updated, bad POST → 400 (trial values reverted); Playwright: pane/tooltips/dropdown/save→file/revert, **0 console errors**; live WS broadcast E2E verified (open session receives the updated frame on save). Follow-up fix (14:53→15:45): the pane appeared stuck open — `.settings { display:flex }` overrode the UA's `dialog:not([open]) { display:none }`, so `close()` left it rendered; scoped the flex layout to `.settings[open]`. Verified with Playwright: hidden on load, closes via button, backdrop click, and real Escape key, 0 console messages.
- T018 central configuration (D014): new `vivo.toml` at the project root — `[llm] [persona] [filler] [voice] [stt] [vad] [barge_in] [memory] [agent]`, every key at its current default and commented; replaces ~20 scattered env vars plus hardcoded values (agent voice-UX prompt, filler phrases, TTS voice/speed/pause, STT params incl. beam_size, all six VAD params, UI barge constants). `app/config.py` = stdlib `tomllib`, precedence **env > file > built-in default**, empty env = unset (compose passes `${VAR:-}` for every key; `CONFIG_PATH` selects the file). `SYSTEM_PROMPT` file-only; `[barge_in]` pushed to the browser in a new `config` WS handshake message (first frame on connect; client keeps defaults as fallback). Dockerfile COPY + compose `:ro` mount → host edit + `docker compose restart vivo`. 7 new tests (`tests/test_config.py`); all 76 prior tests unchanged (wiring flows from config) → **83/83 in 45.95 s**; live handshake verified (`config` frame carries the vivo.toml barge values).
- T017 UI auto barge-in (D013, backfilled — commit `870c22f`): while vivo speaks, the browser sends `barge_in` on its own when the post-echo-cancellation mic level sustains above 0.25 for 250 ms (700 ms cooldown after any barge); manual button unchanged; client-side by necessity (the server's mic stream has no AEC and would self-trigger on vivo's own voice). Thresholds now come from the server's `config` handshake (T018). Auto-trigger awaits the real-mic smoke test (headless has no mic).
- T016 breath-tolerant VAD endpointing (D013, backfilled — commit `31e17ae`): two-stage endpointing in `app/vad.py` — silence past `min_silence_ms` (400) makes the endpoint *pending*; speech resuming within the additional `reopen_ms` (600) continues the same utterance (breath absorbed), else it finalizes at 1.0 s total; `reopen_ms=0` = classic. 2 new offline tests (breath keeps one utterance; 1.4 s pause still splits).
- T015 thinking model + spoken fillers (D012): agent runs the LLM in thinking mode (`LLM_THINKING=1` default; `LLM_MAX_TOKENS` 300→600 — thinking tokens count against the cap) via `chat_template_kwargs={"enable_thinking": …}`; reasoning tokens (`reasoning`/`reasoning_content` deltas) stream as a dedicated `ReasoningDelta` — never spoken, never sent to the client, never stored in history. `ThinkingFiller` daemon thread per reply: after `THINK_FILLER_FIRST_AFTER` (2.0 s) of silence (no speakable text — covers prefill, reasoning, tool rounds) it speaks a random short filler phrase, repeating every `THINK_FILLER_INTERVAL` (8.0 s) while the silence goes on; non-blocking enqueue (full queue = skip tick) and generation-gated, so barge-in stops it with zero stale audio. TTS-queue items are now `(n, sentence, kind)` (kind `tts`/`filler`): reply audio keeps the `tts_N_*` bench names so `bench.py` correlation is untouched; `first_audio` metric filters to `tts_*_sent` only; new bench events `thinking_start`/`thinking_end` + `filler_N_*`. Env: `LLM_THINKING`, `LLM_MAX_TOKENS`, `THINK_FILLER_FIRST_AFTER`, `THINK_FILLER_INTERVAL` (compose + README updated). Verified: **76/76 suite in 64.98 s** — 6 new offline filler tests (`tests/test_thinking_fillers.py`: no filler on fast reply, filler while thinking, repeated fillers, barge-in stop with 0 leaked frames/threads, scheduler suppression/repeat) + 5 new agent tests (thinking payload on/off, reasoning separation via both field names, across tool rounds, live thinking mode).
- T014 decoupled LLM/TTS (D011): per-reply bounded TTS queue (`TTS_QUEUE_SIZE=2`, env) + single in-order TTS worker; LLM producer blocks only while the queue is full; barge-in drains the queue (preserving the `_TTS_DONE` sentinel) and every send is generation-gated before/after/before-send; `SENTENCE_MAX_CHARS` 180→90 (a 180-char chunk is ~11 s of audio and was the dominant first-audio cost). Measured effect (repeats 2, `benchmarks/`): long mode `llm_blocked_on_tts` **8.1–8.4 s → 0.000 s**, `llm_gen_total` 9.7–10.3 s → 1.1–1.2 s, first_audio **11.3–11.7 s → 7.3–7.9 s**; barge first_audio 10.9–13.4 s → **7.4 s** (best run), `cancelled=True` deterministic, 0 stale bytes (baseline barge raced the next LLM delta — `cancelled=False`). 5 new offline tests incl. a regression for a bench-time discovery: a drain that ate the sentinel deadlocked worker + producer (threads leaked, no cleanup). Full suite **63/63 in 64.4 s**.
- T013 latency benchmarking (D011): `Bench` per-utterance recorder in `app/pipeline.py` (phase events + `t0_wall`, one `bench gen=<n> {json}` line) + host-side `bench.py` (scripted WS client, modes reply/long/barge, `--repeats`, docker stats + nvidia-smi sampling correlated to server events, client-wall fallback, JSON reports under `benchmarks/`). Instrumented baseline + after runs captured; five harness bugs fixed (binary-frame fallthrough crash, logs-on-stderr, `docker logs --since` UTC, `Bench.report` self-deadlock, barge log polling).
- T012 full agent tools (D010): nanobot's ExecTool/WebSearchTool/WebFetchTool patterns ported standalone (~300 lines + `ddgs`). `exec` runs in the container as appuser with workspace cwd (host `/home/rob/projects` mounted), 60 s default / 120 s max timeout with process-tree kill, head+tail output truncation, nanobot deny patterns (`rm -rf /`, `mkfs`, `dd if=`, `> /dev/sda*`, `shutdown`, fork bomb…) and a protected-dirs rule (`/app`, `/data`, `/models` read-only). `web_search` = keyless DuckDuckGo; `web_fetch` = Jina Reader markdown with direct-fetch fallback + untrusted-content banner. New `write_file`/`list_dir`; `read_file` now rooted at the workspace; 8 tool rounds; system prompt enforces voice UX (summarise, never read raw output aloud; announce slow tools; retry once). Verified: **58/58 suite**; live LLM smoke ALL PASS — counted 6 workspace dirs, `uname -a` via exec, Iceland population via web_search (~390k), example.com via web_fetch, write+read round-trip on the host.
- T011 conversation memory (D009): selective port of nanobot patterns (history + idle-time summary compaction + parallel tool rounds), not the framework. New `app/conversation.py` (shared `Conversation`: summary + turns, atomic JSON persistence, background compaction that never blocks an utterance); `app/agent.py` `reply(..., history=)`, parallel tool execution, tool-error feedback, non-streaming `summarize()`, max rounds 5; pipeline records each spoken reply as a turn. Env: `COMPACT_AFTER_CHARS` (12000), `KEEP_RECENT_TURNS` (4). Verified: **31/31 suite**; live WS smoke — U1 pangram, U2 "What did I just say?" → **"You said the quick brown fox jumps over the lazy dog."**; live compaction (threshold 200) → 1 checkpoint + 2 recent turns via real LLM. History reset to empty afterwards.
- Barge-in race fixed (D008): `test_barge_in_cancels_reply` was timing out when the interrupted worker was mid an uninterruptible TTS synthesis — `reply_active` stayed up until the old worker exited, so the next utterance's mic audio was dropped by the echo guard and never produced an `end`. `barge_in` now calls `session.release()` (atomically frees the mic + bumps a `generation` counter) and workers check `session.alive(gen)` instead of the old shared cancel Event, so an interrupted reply can never send stale audio and can't clobber a newer one. Verified: barge-in 3/3 alone (25–31 s), full suite **18/18 in 44.95 s** (was flaky at ~150 s).
- Mic/secure-context UX: the browser mic (`getUserMedia`) only exists on HTTPS or `localhost`; over a plain-HTTP LAN IP `navigator.mediaDevices` is `undefined` and the start button threw a cryptic error. Now the UI detects a non-secure context, disables the start button, and posts a clear, actionable sidebar message (HTTPS/reverse-proxy, `localhost`, or the Chrome `unsafely-treat-insecure-origin-as-secure` flag). Verified via Playwright: `http://192.168.0.200:8600` → `startDisabled:true` + message; `http://localhost:8600` → enabled, no false alarm. README gained a "Microphone & HTTPS" section. A self-signed-HTTPS/entrypoint path was prototyped then reverted — the user fronts the container with **Nginx Proxy Manager + a real certificate** (container stays plain HTTP). NPM needs Websockets enabled + a raised upstream timeout for the long-lived WS.
- T010 polish (final): `PERSONA` env verified both ways (pirate → "…arr… matey", default → friendly pangram reply); error surfacing verified end-to-end via dead-LLM ephemeral container → sidebar `entry-error` "ConnectError: [Errno 111] Connection refused"; added WS-disconnect sidebar error; added per-utterance latency log (`utterance X (stt X, first text X, first audio X)`).
- T009 models-on-volume idempotency verified: `docker compose down && up` → `model present: /models/kokoro/…` ×2, zero download lines; forced faster-whisper load (1.15 s, transcribed fixture); all 600 MB volume files md5- and mtime-identical before/after. Removed stray `models/testwrite` leftover.
- T008 `static/{index.html,style.css,app.js}`: browser UI — canvas wobbly audio-reactive dot (state colour, eyes/blink, level-driven wobble+glow), mic capture (getUserMedia → ScriptProcessor → linear resample → 16 kHz int16 → WS binary), playback (24 kHz int16 → chained BufferSources + AnalyserNode), transcript sidebar (you/toy-streaming/tool/error), status pill, start/barage-in/flush/clear controls, inline SVG favicon. Headless-verified via Playwright (`.playwright-mcp/`): transcript + streamed reply + TTS-to-completion + level meter, **0 console errors**.
- T007 `app/pipeline.py` + `app/main.py` `/ws`: WS pipeline (VAD→STT→agent→TTS), sentence chunker, threaded utterance worker, barge-in (cancel Event; mic ignored while reply active as echo guard), `Engines` shared singletons in lifespan. Integration tests `tests/test_pipeline.py`: 2/2 live (happy path + barge-in + post-barge recovery). Fixture `tests/fixtures/prompt_long.wav` (TTS story prompt) added.
- T002 `app/vad.py`: streaming Silero v6 VAD with endpointing (hysteresis, min_speech on voiced region, max-speech split, flush). 5/5 tests.
- T003 `app/stt.py`: faster-whisper small int8 wrapper. Bench 4.6× realtime.
- T004 `app/tts.py`: kokoro-onnx wrapper, per-chunk streaming. Bench ~1.08× realtime (61.8 ms/char).
- T005 `app/agent.py`: hand-rolled streaming tool loop; live test against `qwen3.8-27b` passes (tool round-trip, no think tags).
- T006 `app/tools.py`: get_time / weather / read_file (sandboxed). 6/6 tests.
- `app/models.py`: idempotent kokoro download on startup (`ensure_models` in main.py lifespan); kokoro-v1.0.int8.onnx + voices-v1.0.bin on `./models` (136 MB).
- Test fixture `tests/fixtures/stt_sample.wav` (TTS-generated "The quick brown fox…", 24 kHz int16) committed for STT + VAD tests.

## Blockers
- <none>

## Next Action
- T022 is **done + verified** (docs, `rm -rf models/kokoro`, rebuild, full suite 131/131, live settings-pane smoke all complete). Remaining: **commit** T021 + T022 (working tree uncommitted since `619c75b`) and run the **real-mic smoke** through the NPM HTTPS host (validates mic capture *and* the T017 auto-barge trigger + T022 cloned-voice reply on a real device — untestable headless).
- Commit T021 (working tree uncommitted since `619c75b` — user to confirm) and then T022. T020 was committed as `619c75b`; T019 as `462b00b`; T016–T018 as `46d9f68`. Untracked T020 bench artifact `benchmarks/baseline-thinking-20260916-201355.json` — decide whether to commit or gitignore.
- Top remaining latency lever is now **LLM thinking time** (2.8–24.6 s, contention-noisy on the shared host LLM), which dominates absolute first-audio in thinking mode; the TTS side is already clause-chunked (D016). Options if pursued: a faster thinking model / lower `max_tokens`, or streaming the *reasoning* into a "thinking…" audio cue.
- Candidate follow-ups (out of scope unless asked): `edit_file`/`grep`/`find_files` tools (nanobot search.py patterns), background/long-running command sessions, tightening the compaction summariser prompt, long-term cross-session memory (nanobot `memory.py` consolidation pattern).

## Resume Notes
- Live llama.cpp: `http://localhost:8080/v1` (host) / `http://host.docker.internal:8080/v1` (container). Loaded model `qwen3.8-27b`. Generation ~11 tok/s.
- Run tests: `docker compose run --rm -w /app vivo python -m pytest tests -v -p no:cacheprovider` (fresh container = current image; `exec` on the running container may lag a rebuild).
- Run one-off scripts in-container: copy to `/tmp` then `docker compose exec -T -w /app vivo env PYTHONPATH=/app python /tmp/script.py` (script dir, not cwd, is on sys.path).
- Volume dirs `./models` `./data` must be owned by uid 1000 (docker creates them as root otherwise; `sudo chown 1000:1000` — done).
- VAD test signal: real TTS speech fixture. Synthetic tones do NOT reliably trigger Silero v6 (220 Hz sine max prob 0.507, 1 window; 440 Hz / saw / pink noise: 0) — see NOTES.md.
- LuxTTS (T022): import `from zipvoice.luxvoice import LuxTTS`; CPU loader hardcodes `snapshot_download('YatharthS/LuxTTS')` + loads `openai/whisper-tiny` (both resolved through the HF cache — set `HF_HOME`); `encode_prompt(path, duration=5, rms=0.001)`; `generate_speech(...)` → 48 kHz float **tensor** (use `.detach().cpu().squeeze().numpy()`). Voices are reference clips (3–30 s, mono, 24 kHz int16 on store); each is encoded once on first use or by `prime()`. **Gotcha:** `generate_cpu` hard-multiples the speed by ×1.3, so very short text (≲3–4 words) gets a *negative* frame budget → vocoder conv throws; `synthesize` guards this via `_pick_speed`/`_new_frames` (D018). Supersedes the kokoro-onnx notes (D002/D007).
- TTS is ~0.2× realtime (LuxTTS) vs kokoro's ~1.08×; stream per sentence/chunk to keep first-audio latency low (D018).
- Pipeline WS protocol is documented in the `app/pipeline.py` module docstring (client→server: binary 16k int16 PCM, `barge_in`/`flush`/`ping`/`session` [create] / `session`+`id` [switch]; server→client: `session` [first frame — bound conversation id]/`config` [barge_in timings from vivo.toml]/`start`/`end`/`transcript`/`agent_text`/`tool`/binary **48k** int16 PCM/`barge_ack`/`reply_done`/`error`).
- Pipeline tests: `docker compose run --rm -w /app -e WS_URL=ws://vivo:8000/ws vivo python -m pytest tests -v` (run container reaches the service by compose network name).
- UI test (headless, no mic device): `.playwright-mcp/ui_test*.js` drive the live UI and inject `tests/fixtures/stt_sample.wav` through the page's own `resampleTo16k`→`f32To16`→`ws.send` path (skips only `getUserMedia`). Regenerate the runnable copy (fixture is embedded as base64) after editing the template `ui_test.js`: replace `__PCM_B64__` with the wav body (skip 44-byte header) base64. Served files are baked into the image (`COPY static/`) → `docker compose build && up -d --force-recreate` after UI edits.
- UI host port 8600. User's process: SPEC.md reviewed before scaffolding — satisfied.
