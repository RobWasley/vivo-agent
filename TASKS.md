# TASKS: vivo

> Last updated: 2026-09-18

## Tasks

| ID | Priority | Deps | Description | Verification | Status |
|----|----------|------|-------------|--------------|--------|
| T001 | P0 | — | Scaffold project: Dockerfile (python:3.11-slim, CPU wheels for faster-whisper + kokoro-onnx + onnxruntime), docker-compose.yml (single service, models volume, env LLM_BASE_URL/LLM_MODEL/PERSONA), .gitignore, README. | `docker compose build` succeeds; `docker compose up -d` starts a container that serves a placeholder page on the fixed port. | done |
| T002 | P0 | T001 | Implement Silero VAD wrapper (`app/vad.py`): consume 16k mono PCM frames, emit speech-start/speech-end with endpointing (min speech, max silence). | Unit test: feed synthetic PCM (silence + tone + silence) → correct start/end events with expected timing. | done |
| T003 | P0 | T001 | Implement faster-whisper STT wrapper (`app/stt.py`): lazy-load small int8, transcribe a 16k PCM segment → text. Bench latency on host CPU. | Unit test: transcribe a generated TTS sample (or recorded clip) → expected words present. Record tok/s-equivalent (chars/s) in NOTES.md. | done |
| T004 | P0 | T001 | Implement kokoro-onnx TTS wrapper (`app/tts.py`): lazy-load 82M, synthesize text → 24k PCM, streaming per sentence. Bench latency. | Unit test: synthesize "Hello world" → non-empty PCM at 24kHz. Record ms/char in NOTES.md. | done |
| T005 | P0 | T001 | Implement stateless agent (`app/agent.py`): hand-rolled tool loop against llama.cpp v1 `/chat/completions`, `chat_template_kwargs={"enable_thinking": false}`, streaming, tool-call parsing + re-call. | Curl test against live server: request with enable_thinking=false returns no reasoning; agent loop completes a tool round-trip (get_time) and returns final text. | done |
| T006 | P0 | T005 | Implement tools (`app/tools.py`): get_time, weather (open-meteo, no key), read_file (sandboxed to mounted dir). | Unit tests: get_time returns current time; weather returns JSON for a lat/lon; read_file reads a file in the sandbox and rejects paths outside it. | done |
| T007 | P0 | T002,T003,T004,T005,T006 | Wire the pipeline in `app/main.py`: WS endpoint, VAD→STT→agent→TTS loop, sentence chunker, barge-in (cancel playback + abort LLM). | Integration test: scripted WS client sends synthetic speech → receives transcript + TTS audio; barge-in mid-playback cancels. | done |
| T008 | P1 | T007 | Build the UI (`static/`): wobbly audio-reactive dot (canvas), mic capture (getUserMedia → PCM 16k), audio playback (PCM 24k), transcript sidebar. | Browser test: open UI, speak → transcript appears, dot wobbles with audio, TTS plays. | done |
| T009 | P1 | T007 | Models on volume: download faster-whisper small + kokoro 82M + silero VAD into the mounted volume on first run; skip if present. | `docker compose down && up` → second start does not re-download (log line + no network). | done |
| T010 | P2 | T008 | Polish: persona env honoured, error surfacing in UI, latency logging. | Change PERSONA env → different spoken character; errors visible in sidebar. | done |
| T011 | P1 | T007 | Conversation memory (D009): persistent turn history + idle-time summary compaction (nanobot patterns ported standalone), parallel tool rounds, tool-error feedback. Env: `COMPACT_AFTER_CHARS`, `KEEP_RECENT_TURNS`. | Unit tests (persistence, compaction, races, parallel tools, error feedback) + live WS smoke: 2nd utterance "What did I just say?" references 1st; live LLM compaction checkpoint. | done |
| T012 | P1 | T011 | Full agent tools (D010): sandboxed shell `exec` (container, workspace cwd, deny patterns, protected dirs, timeouts), keyless `web_search` (ddgs), `web_fetch` (Jina reader + fallback, untrusted banner), `write_file` + `list_dir`, read_file root → workspace, 8 tool rounds, capability/voice-UX prompt. Env: `WORK_DIR`, `EXEC_TIMEOUT`, `EXEC_MAX_OUTPUT`, `SEARCH_MAX_RESULTS`, `FETCH_MAX_CHARS`. | Unit tests (deny/timeout/tree-kill/truncation, web fakes + live ddgs, file tools, multi-round loop) + live LLM smoke: all five new tools correctly chosen and used end-to-end. | done |
| T013 | P1 | T007 | Latency benchmarking (D011): per-utterance phase-event recorder in the pipeline (`Bench`, logs `bench gen=<n> {json}` with `t0_wall`); host-side `bench.py` — scripted WS client (modes `reply`/`long`/`barge`, `--repeats`), `docker stats` + `nvidia-smi` resource sampling correlated to server events (with client-wall fallback), JSON reports under `benchmarks/`. | Instrumented baseline + after runs for all three modes (repeats 2) committed to `benchmarks/`; report parses server lines; suite unaffected. | done |
| T014 | P0 | T013 | Decoupled LLM/TTS (D011): bounded per-reply TTS queue (`TTS_QUEUE_SIZE`, default 2) with one consumer synthesizing strictly in enqueue order; producer enqueues finished sentences and blocks only while full (`llm_blocked_on_tts` measured); cooperative barge-in — `release()` bumps generation + drains queue (preserving the `_TTS_DONE` sentinel) and every send is gated on `alive(gen)` before synth / after synth / before send; `SENTENCE_MAX_CHARS` 180→90 (env-configurable). | 5 new offline tests (order, LLM/TTS overlap, queue bound, barge recovery, post-completion barge deadlock regression) + live barge bench (cancelled=True, 0 stale bytes); full suite **63/63**. | done |
| T015 | P1 | T014 | Thinking model + spoken fillers (D012): agent runs the LLM in thinking mode (`LLM_THINKING` default 1; `LLM_MAX_TOKENS` 300→600 — thinking tokens count against the cap); reasoning tokens stream as `ReasoningDelta` (never spoken, never sent to the client, never stored in history); `ThinkingFiller` speaks a short filler phrase after `THINK_FILLER_FIRST_AFTER` (2.0 s) of silence and repeats every `THINK_FILLER_INTERVAL` (8.0 s) while the silence goes on; filler audio excluded from the `first_audio` bench metric; bench events `thinking_start`/`thinking_end` + `filler_N_*`; env: `LLM_THINKING`, `LLM_MAX_TOKENS`, `THINK_FILLER_FIRST_AFTER`, `THINK_FILLER_INTERVAL`. | 6 new offline filler tests (no filler on fast reply, filler while thinking, repeated fillers on long thinking, filler stops on barge-in with no leaked threads/audio, scheduler suppression/repeat) + 5 new agent tests (thinking payload on/off, reasoning deltas separated from text incl. both server field names, across tool rounds, live thinking mode); full suite **76/76 in 65 s**. | done |
| T016 | P1 | T002 | Breath-tolerant VAD endpointing (D013, backfilled): two-stage endpointing — after `min_silence_ms` the endpoint goes *pending* and opens an additional `reopen_ms` window (default 600); speech resuming inside the window continues the same utterance (a breath is absorbed), silence finalizes at `min_silence_ms + reopen_ms`; `reopen_ms=0` = classic single-stage. Env: `VAD_REOPEN_MS`. | 2 new offline VAD tests (0.6 s breath pause keeps one utterance; 1.4 s pause still splits) — included in the 76/76 suite run; VAD params later centralized in vivo.toml `[vad]` (T018). | done |
| T017 | P2 | T008 | UI auto barge-in (D013, backfilled): while vivo is speaking the browser watches the per-callback mic level (rms×6 clamped 0–1, ~25 ms sampling, post-browser-echo-cancellation) and sends `barge_in` itself when it stays above `level_threshold` (0.25) for `sustain_ms` (250) — with `cooldown_ms` (700) suppressing re-triggers right after any barge; manual button unchanged; server-side barge machinery (D008) untouched. | State machine in `static/app.js` (headless-verified paths: manual barge covered live by `tests/test_pipeline.py`; auto-trigger needs a real mic — pending with the NPM HTTPS smoke test); thresholds moved to vivo.toml `[barge_in]` and pushed to the client in T018. | done |
| T018 | P1 | T017 | Central configuration (D014): single `vivo.toml` at the project root (sections `[llm] [persona] [filler] [voice] [stt] [vad] [barge_in] [memory] [agent]`) replaces scattered env vars + hardcoded values (agent voice-UX system prompt, filler phrases, TTS voice/speed/pause, STT model/compute/threads/language/beam, VAD threshold/pads/max-speech, UI barge constants); `app/config.py` loads it with stdlib `tomllib`, precedence **env > file > built-in default** (empty env = unset, so compose passes `${VAR:-}`); every key keeps its historical env name; `SYSTEM_PROMPT` file-only; `[barge_in]` pushed to the browser in a new `config` WS handshake message (client falls back to defaults); `MODEL_DIR`/`DATA_DIR`/`WORK_DIR` stay env-only in compose; Dockerfile COPY + compose `:ro` mount → host edit + `docker compose restart vivo`. | 7 new tests (`tests/test_config.py`: missing-file defaults, file values, env-overrides-file, empty-env-unset, bool parsing, comma-separated list, shipped vivo.toml completeness) + all 76 pre-existing tests unchanged (prompt/filler/engines now flow from config) → full suite **83/83 in 46 s**; live: service recreated, `/health` OK, first WS frame is the `config` handshake with vivo.toml values. | done |
| T019 | P1 | T018 | Settings pane (D015): web settings dialog (gear button) rendered client-side from a server typed schema — new `app/config_schema.py` `SCHEMA` (all 9 sections / 36 keys: type, label, tooltip, min/max/step, choices, per-key apply semantics) is the single source for the pane, server validation, and the file's comments; controls per type (sliders with live readout, dropdowns incl. kokoro voices fetched from the loaded engine, checkboxes, phrases textarea). `GET /api/config` → `{values, schema, voices, path}`; `POST /api/config` → validate (400 with per-key error list) → coerce → merge partial updates into the full effective dict → **write back to the host `vivo.toml`** (`write_config`, direct write — the file mount can't `os.replace` across filesystems) → `refresh()` → `pipeline.apply_config()` for `now` keys (LLM, persona, filler phrases, TTS voice/speed/pause, STT language/beam, memory, agent + exec/web limits incl. the `shell.MAX_TIMEOUT` snapshot, filler list) → `pipeline.broadcast_config()` re-sends the `barge_in` frame to every open WS session; `next`/`restart` keys are written and take effect later. Compose mount flips to read-write; loader tolerates a corrupt file (warn + defaults). | 12 new tests (`tests/test_config_api.py`: schema↔shipped toml, schema attrs↔effective values, validate good/bad, writer round-trip incl. string escaping, refresh picks up edits, corrupt-file tolerance, offline GET/POST on bare FastAPI with fake engines, broadcast reaches a fake open session, live `TestClient` round-trip) → full suite **95/95 in 79.8 s**; live: `GET` shows 54 kokoro voices, `POST tts_speed=1.25` → 200 + file write on host, invalid POST → 400; UI verified via Playwright (pane render, tooltips, 54-voice dropdown, save → host file updated, revert, 0 console errors); live WS broadcast verified (open session receives the updated `config` frame on save, revert). | done |
| T020 | P1 | T015 | Sub-sentence TTS streaming — clause chunking (D016): `SentenceChunker` splits an in-progress sentence at clause punctuation (comma/semicolon/colon) once the buffer exceeds `[voice] clause_max_chars` (env `CLAUSE_MAX_CHARS`, default **40**, `0` = off, apply `next`) so TTS starts on the first clause instead of waiting for the sentence ending; the split takes the **last** boundary in the buffer (guards: a boundary nearer than half the threshold waits for more text) and the 90-char hard split stays the no-punctuation backstop. Spoken text unchanged — only chunk boundaries move. Chunker built per utterance from config → settings-pane knob. | 11 new tests (`tests/test_clause_chunking.py`: 10 unit — endings win, clause before sentence completes, threshold guard, multi-clause order, `;`/`:` boundaries, near-start wait, hard-split backstop `[90, 30]`, `0` disables, default legacy — + 1 pipeline: first clause reaches TTS before the sentence completes) → full suite **106/106 in 102 s**. Fresh thinking-on bench (repeats 2, reply/long/barge; host LLM shared with the session — thinking 2.8–24.6 s noisy): TTS-side first audio (`llm_first_token`→first `tts_*_sent`) **3.7–8.6 s → 4.5–5.7 s**; first-chunk synth **3.4–6.9 s → 2.5–5.1 s**; server `first_audio` reply **20.8/30.0 → 10.3/8.8 s**, long **16.7/14.0 → 11.2/13.5 s**; worst inter-chunk gap **2.6 → 2.0 s**; barge-in unchanged (0 stale bytes). Harness fix: `tests/test_pipeline.py` + `bench.py` endpointing tail now derived from the service's effective `vad.min_silence_ms + reopen_ms` (+0.5 s) — the fixed 1–1.5 s tail had stopped endpointing fixtures after the user raised `min_silence_ms` to 2900 (invalidated the first baseline: 0 audio frames). | done |
| T021 | P1 | T011 | Named conversation sessions (D017): `SessionStore` in `app/conversation.py` — many named sessions (ids `YYYYMMDD-HHMMSS` + `-N` collision suffixes, retired ids never reused), one *active*, each a `Conversation` at `./data/sessions/<id>.json` + index `./data/sessions.json` (`{active, sessions:{id:{created,last_used,turns}}}`, turn counts kept fresh via a new `Conversation.on_change` callback); legacy `./data/conversation.json` migrated once on first start; orphan `sessions/*.json` adopted; corrupt index/session files tolerated. WS: `/ws?session=<id>` pins a connection (unknown → active), **first frame** `{"type":"session","id":…}`, `{"type":"session"}` creates / `{"type":"session","id":…}` switches (both also set active; unknown → `error`); a switch affects the next utterance only. REST: `GET/POST /api/sessions`, `POST /api/sessions/{id}/activate`, `DELETE /api/sessions/{id}` (deleting the last one creates a fresh active). UI: footer session dropdown (formatted ids, "(active)" mark) + **new** button; each tab remembers its session in `localStorage`. `[memory]` limits hot-apply per session (`set_limits`). | 22 new offline tests (`tests/test_sessions.py`: store CRUD incl. last-delete-creates-fresh + no-id-reuse, persistence round-trip, on_change index + corrupt-callback tolerance, corrupt index/session ⇒ fresh start, orphan adoption, legacy migration, `Conversation` callback, `VoiceSession` bind/pin/switch/fallback, `serve_session` handshake + create/switch/unknown + `?session=` pin, `/api/sessions` offline CRUD + 404s) → full suite **128/128 in ~83 s** (incl. live WS pipeline tests). Live: image rebuilt + service `--force-recreate` → first start created `data/sessions.json` + `data/sessions/<id>.json` (one active session), `/health` OK, live tests pass against the new server (handshake `session` frame before `config`). | done |

| T022 | P1 | T019 | TTS engine swap + voice cloning (D018): kokoro-onnx → **LuxTTS** (zipvoice, CPU torch + onnxruntime, 48 kHz) — `app/tts.py` rewritten: voices are reference clips (`.wav` in `<data_dir>/voices`, 3–30 s, mono, resampled to 24 kHz int16 on store), each encoded once on first use (whisper-tiny transcription + feature extraction, `encode_prompt`) or pre-encoded by `prime()` after upload; per-sentence `generate_speech` streaming at ~0.2× realtime; `sentence_pause_s` now appends trailing silence per chunk (the setting becomes functional). Bundled `voices/default.wav` (af_heart speech) seeded into `data/voices/` by `app/models.py` (`ensure_models` now pre-fetches the HF repos `YatharthS/LuxTTS` + `openai/whisper-tiny` into `HF_HOME`, compose sets `HF_HOME=/models/hf`; the CPU loader resolves both through the HF cache). Backend: `POST /api/voice` (multipart clip → store + make active + persist + background clone), `GET /api/voices/{name}` (clip preview), `DELETE /api/voices/{name}` (active voice protected). Frontend: voice row gains **upload / record / preview / delete** (recording via MediaRecorder, re-encoded to 16-bit mono WAV in the browser), playback rate 24→48 kHz. `TTS_CPU_THREADS` env-only (default 8). | tests: `tests/test_tts.py` rewritten (synthesize/stream/voices/save+delete incl. range + format rejection) + live `test_api_config_live` exercises upload/preview/409/delete against the real engine; fakes + bench moved to 48 kHz; **short-text guard** (D018): `synthesize` predicts the LuxTTS frame budget and lowers per-utterance speed (floor 0.6) so ≲3–4-word sentences don't underflow the vocoder and crash the per-sentence worker, and never raises (empty → worker skips). Full suite **131/131** in the app image (the single red run was a transient external Open-Meteo `503` in `test_weather`, green on re-run); live smoke: `POST /api/voice` upload→activate (`duration_s`), `GET` preview (RIFF/WAV), active-delete `409`, missing-delete `404`, voice restored to `default`; bench RTF 0.20–0.21, clone quality user-confirmed. | done |
| T023 | P1 | T019 | User profile (D019): new `[user]` section in `vivo.toml` (settings-pane row + env `USER_NAME`/`USER_LOCATION`/`USER_TIMEZONE`/`USER_UNITS`, all `apply: "now"`): name, place-name location (e.g. "Bristol, UK"), IANA timezone, `units` (metric/imperial). `get_time()` reports the profile's timezone (`zoneinfo` + new `tzdata` image dependency; falls back to container-local on empty/unknown zone). `weather()` takes an optional place name — defaulting to the profile's location, resolved via open-meteo's keyless geocoding API (in-process cache) — and reports in the preferred units (imperial → °F/mph). The profile block is appended to the agent's system prompt after the persona (empty when unset). | 9 new/rewritten tests (tz-aware get_time incl. invalid-zone fallback; geocoded weather: default location, named location, imperial, no-location error, unknown-place error, geocode caching; profile-in-prompt + `user_profile()` config wiring) → full suite **142/142** (140 in `docker compose run` + the 2 live-WS pipeline tests via `-e WS_URL=ws://vivo:8000/ws`). | done |
| T024 | P1 | T019 | Wake phrase (D020): `app/wake.py` `WakeState` (shared, hot-applied) gates the pipeline — with a non-empty `[wake] phrase` vivo starts **asleep**: every utterance is still transcribed, but only one containing the phrase (words in order, punctuation/case tolerant) reaches the LLM — everything else is dropped silently (no transcript frame, no reply, nothing stored). Phrase-only ⇒ spoken `ack` with no LLM round trip; phrase+request ⇒ ack spoken first, then the remainder runs the normal LLM/TTS loop (transcript frame keeps the full utterance). An awake session ends on a configured `end_phrases` match or when `session_timeout_s` elapses without an exchange (checked from the audio path, suppressed while any reply is active) — both speak the `goodnight` to all open sessions. Empty phrase = feature off = legacy always-answer. `[wake]` section (phrase / session_timeout_s / end_phrases / ack / goodnight, all `apply: now`, env `WAKE_*`, hot-applied via `engines.wake.update()`). Protocol: server→client `{"type":"wake","active"}` (handshake frame after `session`+`config`, plus broadcast on state change); client→server `{"type":"wake"}` manual wake. UI: `asleep` status state (slate LED) + phrase hint + wake button; asleep `start`/`end` frames suppressed client-side. | unit (`tests/test_wake.py`: matching incl. punctuation/word boundaries, empty-phrase off, hot-apply, wake/sleep/claim_sleep with fake clock) + pipeline fakes (silent drop, ack-only with **no LLM call**, ack+remainder reply, end phrase ⇒ goodnight+sleep, idle timeout ⇒ goodnight+sleep, timeout suppressed mid-reply, disabled legacy) + live WS via `manual_wake()`; test fakes given disabled `WakeState()`; drive-by fix: `test_tts_queue.py` barge-timestamp test was already red on HEAD (session built outside an event loop + asserted `release()` clears what it records) → rewritten to drive the real `_handle_utterance` finally path. Full suite **172/172** (170 offline + 2 live). | ✅ 2026-09-18 |

## Status Legend
- `pending` — not started
- `in-progress` — actively being worked
- `blocked` — cannot proceed (see Blockers)
- `done` — complete and verified (evidence recorded)
- `descoped` — removed from scope (record why)

## Blockers
- <none>

## Dependency Notes
- T001 done 2026-09-15 16:34: `docker compose build` OK; container serves placeholder UI on host port 8600 (in-container 8000), `/health` OK. All engine imports verified in-image (ctranslate2 4.8.2, onnxruntime 1.30.0 CPU, kokoro_onnx 0.6.1). faster-whisper bundles `silero_vad_v6.onnx` → no separate VAD package needed (see D006).
- T002–T006 done 2026-09-15 17:52: full suite `docker compose run --rm vivo python -m pytest tests -v` → **16 passed in 17.8s** (test_vad 5, test_tts 3, test_stt 1, test_agent 1 live LLM, test_tools 6).
  - T002: `app/vad.py` streaming Silero v6 (onnx, 512-sample windows + 64-sample context + LSTM state), endpointing with hysteresis (0.5/0.35), min_speech applied to the voiced region, max-speech split, `flush()`. Tests use the real TTS speech fixture (synthetic tones proved unreliable — see NOTES).
  - T003: `app/stt.py` faster-whisper small int8. Bench: 2.84 s audio in 0.61 s = **4.6× realtime** (NOTES.md).
  - T004: `app/tts.py` kokoro-onnx. Bench: 180 chars → 11.1 s, 61.8 ms/char ≈ **1.08× realtime** (NOTES.md; D007).
  - T005: `app/agent.py` verified against live `qwen3.8-27b` — tool round-trip (get_time) + no think tags, streaming deltas.
  - T006: `app/tools.py` all 6 tests pass incl. path-escape rejection.
- T007 done 2026-09-15 18:35: `app/pipeline.py` (Engines shared singletons, SentenceChunker, VoiceSession, threaded utterance worker with `run_coroutine_threadsafe` sends) + `app/main.py` `/ws` endpoint + lifespan `Engines`. Integration tests `tests/test_pipeline.py` (scripted `websockets` client vs live service): happy path (start→end→transcript≥5/7 words→agent_text→24k TTS binary→reply_done) and barge-in (mid-reply cancel → barge_ack, no further TTS, fresh utterance still works). Full suite **18/18 in 50.4s** incl. live LLM. Latency: first audio ≈ STT (~0.7 s steady) + LLM first sentence (~1.5–2 s) + TTS sentence (~1.6–3 s) after speech end (NOTES.md).
- T008 done 2026-09-15 19:12: `static/{index.html,style.css,app.js}` — canvas wobbly audio-reactive dot (state-coloured, eyes+blink, level-driven wobble/glow), mic capture (getUserMedia → ScriptProcessor → linear resample to 16 kHz int16 → WS binary, any device rate), playback (24 kHz int16 → chained BufferSources with lookahead scheduling + AnalyserNode level), transcript sidebar (you / toy streaming / tool / error), status pill (connecting/ready/listening/thinking/speaking), controls (start/stop mic, barge in, flush, clear). Verified headless via Playwright (`.playwright-mcp/ui_test*.js`): injected the TTS fixture through the page's own send path → WS open → `transcript` "The quick brown fox jumps over the lazy dog." → `agent_text` reply streamed → 24 kHz TTS decoded + played to completion → meter tracked input level (80%) → **zero console errors** (inline data-URI favicon removes the `/favicon.ico` 404). Mic *capture* itself not testable headless (no device); getUserMedia→PCM path exercised via the same resample/send functions.
- Critical path: T001 → {T002,T003,T004,T005} → T006 → T007 → T008.
- T002/T003/T004/T005 are parallelizable after T001 (independent wrappers).
- T006 depends on T005 (tool schemas live in the agent).
- T009 done 2026-09-15 19:36: kokoro via `ensure_models()` (lifespan), faster-whisper small lazily on first transcription (`download_root=/models`), silero VAD bundled in the faster-whisper package (D006, never downloaded). Verified: `docker compose down && up` → logs show `model present: /models/kokoro/…` ×2 and zero download/fetch lines; forced whisper load in the fresh container (1.15 s, transcribed the fixture); **all 600 MB of volume files md5- and mtime-identical** before/after. Also removed a stray `models/testwrite` debug leftover.
- T009 can run any time after T001 (volume wiring).
- T010 done 2026-09-15 20:02 (polish, final): (1) **persona env** — `PERSONA` in `.env`/compose → `config.PERSONA` → `Agent` system prompt; verified live both ways: pirate persona (`.env`) → "That's the classic tongue-twister, arr, but what be yer true quest, matey?", default after removing it → "…lazy dog! That's the rest of the famous pangram." (2) **error surfacing** — pipeline exceptions already sent as `error` WS events → sidebar `entry-error`; verified end-to-end: ephemeral container with dead `LLM_BASE_URL` (port 9) → browser sidebar shows `ConnectError: [Errno 111] Connection refused` with the transcript still displayed, zero console errors. Added: WS disconnect after a successful connect now also posts a sidebar error ("disconnected from server") — previously only the status pill changed. (3) **latency logging** — `app/pipeline.py` now logs per utterance: `utterance 11.78s (stt 1.78s, first text 5.64s, first audio 8.61s)` (speech-end → first LLM text / first TTS audio / total) alongside the existing per-stage `stt`/`tts` lines. Full suite re-run: 17/18 with one barge-in TimeoutError under heavy LLM contention (suite 151 s vs ~50 s norm; the host LLM serves this session too) — test passes in 34.7 s when run alone; change is logging-only.
- T012 done 2026-09-15 23:49 (D010): new `app/shell.py` (`run_shell`: `bash -c` in-container as appuser, workspace cwd, `working_dir` containment, timeout 1–120 s with process-tree kill, head+tail truncation at `EXEC_MAX_OUTPUT`=8000, nanobot deny patterns + protected-dirs rule for `/app`,`/data`,`/models` writes) and `app/web.py` (`web_search` via keyless `ddgs` ≤5 results, `web_fetch` via Jina Reader with direct-fetch HTML→text fallback, untrusted-content banner, cap 6000/16000). `app/tools.py`: `write_file`, `list_dir` (ignored dirs), `read_file` root → `WORK_DIR`, global 16k result cap. `app/agent.py`: max rounds 8, capability + voice-UX system prompt (summarise don't read aloud; announce slow tools; retry once). Compose: `/home/rob/projects:/workspace` + `WORK_DIR=/workspace` (user-chosen; host uid 1000 = container appuser uid). requirements: `ddgs>=9`. 25 new tests (test_shell.py 11, test_web.py 10 incl. live ddgs, test_files.py 6, +1 multi-round in test_agent.py). Full suite **58/58 in 56 s**. Live LLM smoke ALL PASS: `list_dir` counted 6 workspace dirs; `exec uname -a` → kernel 6.8.0-139-generic; `web_search` Iceland → ~390,000; `web_fetch` example.com; `write_file`+`read_file` round-trip (file verified on host, then removed). History reset to empty, service recreated.
- T015 done 2026-09-16 10:58 (D012): `app/agent.py` — `thinking` param (default `False`; pipeline passes `config.LLM_THINKING`) → `chat_template_kwargs={"enable_thinking": …}`; `_stream_once` parses `reasoning`/`reasoning_content` deltas (the live llama.cpp server emits `reasoning_content`, verified by curl) → `reply()` yields the new `ReasoningDelta` control signal; `summarize()` stays non-thinking. `app/pipeline.py` — `ThinkingFiller` daemon thread (0.25 s tick; non-blocking `q.put(timeout=0.2)`; generation-gated via `alive(gen)`; `stop()` joins before the TTS sentinel); TTS-queue items are now `(n, text, kind)` with kind ∈ {`tts`, `filler`} — worker bench events `{kind}_{N}_*` (reply audio keeps the `tts_N_*` names, so `bench.py` correlation is untouched); `_handle_utterance` handles `ReasoningDelta` (bench `thinking_start`/`thinking_end`, total `thinking` in the report + per-utterance log) and calls `filler.note_text()` on every speakable delta; `Bench.first_audio`/`report` filter to `tts_*_sent` only. `app/config.py` — `LLM_THINKING` (1), `LLM_MAX_TOKENS` (600), `THINK_FILLER_FIRST_AFTER` (2.0), `THINK_FILLER_INTERVAL` (8.0); compose + README updated. Tests: 6 new offline (`tests/test_thinking_fillers.py`) + 4 new offline and 1 new live in `tests/test_agent.py`. Full suite **76/76 in 64.98 s**.
- T014 done 2026-09-16 06:35 (D011): `app/pipeline.py` — per-reply `queue.Queue(maxsize=TTS_QUEUE_SIZE)` created in `start_utterance`; `_tts_worker` (single consumer, `_TTS_DONE` sentinel, alive checks before synth / after synth / before send); `_handle_utterance` producer loop enqueues chunker output, blocks only while the queue is full; `release()` records `barge_mono[gen]`, drains the queue **preserving the sentinel** (a drain that ate `_TTS_DONE` deadlocked worker + producer — regression test `test_barge_after_generation_complete_does_not_deadlock`). `app/config.py`: `TTS_QUEUE_SIZE` (2), `SENTENCE_MAX_CHARS` (90; was hardcoded 180 — a 180-char punctuation-poor chunk is ~11 s of audio and was the dominant first-audio cost, baseline barge first_audio 10.9–13.4 s). New `tests/test_tts_queue.py` (5 offline tests, fake engines/WS). Bench evidence (`benchmarks/`, repeats 2): long mode `llm_blocked_on_tts` **8.1–8.4 s → 0.000 s**, `llm_gen_total` 9.7–10.3 s (wall, incl. TTS waits) → 1.1–1.2 s (pure gen), first_audio **11.3–11.7 s → 7.3–7.9 s** (audio now 2 frames, not 1 giant 8 s frame); barge: first_audio 10.9–13.4 s → **7.4 s** (best run; LLM first-token variance 0.9–5.9 s across runs), `cancelled=True` with `barge_in`→`stale_stop` ≈ one in-flight synthesis (the uninterruptible step, by design D008) and **0 stale audio bytes** client-side, vs the baseline where the reply raced the barge (`cancelled=False`, finished 11 ms before barge processing). Full suite **63/63 in 64.4 s**.
- T013 done 2026-09-16 06:05 (D011): `Bench` recorder in `app/pipeline.py` (thread-safe, monotonic t0 + `t0_wall`, events `utterance_end`/`stt_end`/`llm_first_token`/`sentence_N_ready`/`tts_N_start/end/sent`/`generation_complete`/`barge_in`/`stale_stop`/`reply_done`, extras `first_audio`/`llm_gen_total`/`tts_total`/`llm_blocked_on_tts`/`sentences`/`queue_max`) → one `bench gen=<n> {json}` line per utterance. `bench.py` (host): scripted WS client with the TTS fixtures (reply/long/barge, `--repeats`), `Sampler` thread (docker stats CPU/mem + nvidia-smi util/VRAM), server-event correlation with a client-wall fallback, JSON reports under `benchmarks/`. Harness bugs fixed along the way: binary frames fell through to `json.loads` (100%-repro crash); container logs go to **stderr** (was reading stdout only); `docker logs --since` needs explicit UTC (naive = parsed as UTC); `Bench.report` self-deadlocked on its non-reentrant lock (no line ever logged); barge-mode log polling (server reports after finishing + discarding the in-flight synthesis, seconds after `barge_ack`). Evidence: instrumented baseline `benchmarks/baseline-pure-20260916-060238.json` + after runs `benchmarks/after-queue-20260916-061624.json`, `benchmarks/after-queue-barge-20260916-063027.json` (superseded early captures deleted).
- T011 done 2026-09-15 22:35 (D009): `app/conversation.py` (new) — shared `Conversation` (summary + turns), JSON persistence at `./data/conversation.json` (atomic, corrupt ⇒ fresh), idle-time `compact(summarize)` (background thread; snapshot under lock → non-streaming LLM call without lock → apply only if history unchanged) and `messages()` (summary as leading system message + flattened turns). `app/agent.py` — `reply(..., history=)`, parallel tool execution within a round (ThreadPoolExecutor, order preserved), tool exceptions → `error: …` model-visible text, max rounds 3→5, non-streaming `summarize()`. `app/pipeline.py` — `Engines.conversation`; per utterance: history sent → spoken deltas recorded as a turn → `maybe_compact` fired in background. New env: `COMPACT_AFTER_CHARS` (12000), `KEEP_RECENT_TURNS` (4). Tests: 10 new (test_conversation.py 8: persistence, corrupt-file, thresholds, compaction incl. previous-summary fold + grew-while-summarising skip, background no-op; test_agent.py +4 offline with fake httpx client: history pass-through, no-history unchanged, parallel+error round, max-rounds stop, summarize shape) + existing live round-trip. Full suite **31/31 in 41.9 s**. Live WS smoke: U1 pangram → U2 "What did I just say?" → "You said the quick brown fox jumps over the lazy dog." (REMEMBERED); live compaction at threshold 200: 5 turns → 1 checkpoint + 2 recent via real LLM. Post-smoke, history reset to empty + service recreated.
- T016 done 2026-09-16 ~11:30 (D013; **backfilled 13:06** — committed as `31e17ae` before being documented): `app/vad.py` two-stage endpointing — after `min_silence_ms` the endpoint goes *pending* and opens an additional `reopen_ms` (default 600) window; speech resuming inside it continues the same utterance (breath absorbed), silence finalizes at `min_silence_ms + reopen_ms`; `reopen_ms=0` restores classic single-stage. 2 new tests in `tests/test_vad.py` (`test_breath_pause_keeps_single_utterance`, `test_pause_beyond_reopen_window_still_splits`); existing tests pin `reopen_ms=0`. Part of the 76/76 suite run.
- T017 done 2026-09-16 ~11:50 (D013; **backfilled 13:06** — committed as `870c22f` before being documented): `static/app.js` auto barge-in — while `vivoIsSpeaking()`, the per-callback mic level (`rms×6` clamped 0–1, ~25 ms sampling; post-browser-echo-cancellation: typical speech 0.3–1.0, echo < 0.2) sustaining above `BARGE_LEVEL_THRESHOLD` (0.25) for `BARGE_SUSTAIN_MS` (250) sends `barge_in`; `BARGE_COOLDOWN_MS` (700) after any barge suppresses re-triggers; a level dip restarts the sustain window; manual button and server machinery (D008) unchanged. Client-side by necessity: the server's own mic stream has no AEC (the speaker's TTS would self-trigger), the browser's cleaned level is the only usable signal. Auto-trigger path awaits a real-mic smoke test (headless has no mic device — same constraint as T008).
- T018 done 2026-09-16 13:06 (D014): new `vivo.toml` at the repo root (all sections at their default values, commented) + rewritten `app/config.py` (stdlib `tomllib`; `_get(section, key, env, default, cast)` with precedence **env > file > built-in default**, empty env = unset; `CONFIG_PATH` env selects the file, default `vivo.toml`). New/centralized knobs: `SYSTEM_PROMPT` (file-only; the voice-UX prompt moved out of `app/agent.py`, now `system_prompt=` kwarg appended after the persona blurb), `FILLER_PHRASES` (env `THINK_FILLER_PHRASES`, comma-separated), `TTS_VOICE`/`TTS_SPEED`/`TTS_SENTENCE_PAUSE`, `STT_MODEL`/`STT_COMPUTE_TYPE`/`STT_CPU_THREADS`/`STT_LANGUAGE`/`STT_BEAM_SIZE` (beam_size was hardcoded 1), all six `VAD_*` params (wiring in `VoiceSession` now passes them explicitly), `BARGE_LEVEL_THRESHOLD`/`BARGE_SUSTAIN_MS`/`BARGE_COOLDOWN_MS`, `MAX_TOOL_ROUNDS`, `EXEC_MAX_TIMEOUT`. `serve_session` sends `{"type":"config","barge_in":{…}}` as the first frame on connect; `static/app.js` keeps `BARGE_DEFAULTS` fallback and updates from the message. `Dockerfile` COPYs `vivo.toml`; compose mounts `./vivo.toml:/app/vivo.toml:ro` + `${VAR:-}` passthrough for every key (MODEL_DIR/DATA_DIR/WORK_DIR stay fixed env). All pre-existing `config` public names preserved (nothing else imports changed except the wiring above). 7 new tests in `tests/test_config.py` (reload-based fixture with env snapshot/restore). Full suite **83/83 in 45.95 s**. Live: `docker compose up -d --force-recreate` → `/health` OK, first WS frame `{"type":"config","barge_in":{"level_threshold":0.25,"sustain_ms":250,"cooldown_ms":700}}`.
- T019 done 2026-09-16 14:53 (D015): new `app/config_schema.py` — `SCHEMA` (9 sections / 36 keys; per-key type/label/help/apply/attr/min/max/step/choices) as the single source of truth for the settings pane, server validation, and `vivo.toml` comments; `validate(values, voices)` (per-key error list, unknown sections/keys, numeric ranges, non-empty phrase lists, known-voice check), `coerce()` (integral floats → int), `dump_toml()` (schema-ordered sections with per-key help comments, string escaping). `app/config.py` rewritten around it: tolerant `_load_file()` (TOML/OSError → warn + `{}` defaults — a corrupt file can no longer kill startup), `refresh()`, `effective_dict()`, `write_config()` (single direct write; the file bind mount crosses filesystems so `os.replace` raises EXDEV — atomic rename deliberately not used). `app/tts.py`: `voices()` (54 kokoro voices from the loaded engine). `app/pipeline.py`: `apply_config(engines)` hot-patches `now` keys (agent base_url/model/persona/system_prompt/thinking/max_tokens/max_tool_rounds, tts voice/speed/pause, stt language/beam, conversation compact/keep, `app.shell.MAX_TIMEOUT` — an import-time snapshot — and `FILLER_PHRASES`); module `_ACTIVE` session set + `broadcast_config()` re-sending the `barge_in` `config` frame (registered in `serve_session`, skipped for closed sockets); exec/web limits need no patching (call-time `config.*` reads). `app/main.py`: `GET /api/config` (`{values, schema, voices, path}`) + `POST /api/config` (validate → coerce → merge into full effective dict → `write_config` → `refresh` → `apply_config` → `broadcast_config`; 400 with error list) — registered before the static mount. Frontend: gear button + `<dialog id="settings">`; `static/app.js` settings module renders every control from the schema (typed: `.srange` slider+readout, dropdowns, checkboxes, phrases textarea), `(?)` tooltips, `live`/`next session`/`restart` apply badges, full-snapshot save, message flash; `static/style.css` pane styles. `docker-compose.yml`: `./vivo.toml:/app/vivo.toml` (now read-write). 12 new tests in `tests/test_config_api.py` (schema↔shipped toml, attrs↔effective, validate good/bad, writer round-trip incl. escaping, refresh picks edits, corrupt-file tolerance, offline GET/POST on bare FastAPI with fake engines + `ws.send_text`, broadcast to a fake open session via background loop, live `TestClient` round-trip). Full suite **95/95 in 79.83 s**. Live: service `--force-recreate` → `/health` OK; `GET` returns 9 sections + 54 voices; `POST tts_speed:1.25` → 200 + host file updated; invalid `vad.threshold:0.0` → 400; all trial values reverted. Playwright: pane renders (sections/tooltips/badges/sliders/54-entry voice dropdown), tooltip hover works, save → host `vivo.toml` updated then reverted, **0 console errors**. Live WS broadcast E2E: handshake `config` frame on connect; `POST level_threshold:0.4` → open session receives the broadcast; reverted.
- T020 done 2026-09-16 ~22:05 (D016): `app/pipeline.py` `SentenceChunker` clause-level splitting — `CLAUSE_RE = re.compile(r"[,;:] ")` (boundary = the space after a clause punctuation mark, so the mark stays in the chunk), `clause_max_chars` param (default 0 = off); `_clause_splits()` runs after the sentence-ending loop in `add()`: while `len(buf) > clause_max_chars`, emit text up to the **last** clause boundary in the buffer (streaming deltas are small, so the last boundary sits near the threshold); a boundary nearer than `clause_max_chars // 2` breaks the loop (wait for more text — the 90-char hard split is the backstop). Construction site passes `clause_max_chars=config.CLAUSE_MAX_CHARS`. `app/config_schema.py`: new [voice] key `clause_max_chars` (int, label "Clause split (chars)", apply `next`, min 0, max 200, step 5, attr `CLAUSE_MAX_CHARS`); `app/config.py` emits `CLAUSE_MAX_CHARS` (`_get("voice","clause_max_chars","CLAUSE_MAX_CHARS",40,int)`) + `effective_dict` entry; `docker-compose.yml` `${CLAUSE_MAX_CHARS:-}` passthrough; `vivo.toml` + README `clause_max_chars = 40`. 11 new tests in `tests/test_clause_chunking.py` (10 unit: endings win; clause emitted before the sentence completes; no split below the threshold; multi-clause order; semicolon; colon; near-start boundary waits; 120-char unpunctuated → hard split `[90, 30]`; `0` disabled; default legacy) + 1 pipeline test with a fake timing TTS: the first clause's `tts_start` lands before the sentence's `sentence_ready` (imports the `test_tts_queue` harness). Harness fix: `tests/test_pipeline.py::tail_silence_samples()` and `bench.py::tail_silence_samples()` now fetch the service's effective `vad.min_silence_ms + reopen_ms` (`GET /api/config`, derived from the WS URL's netloc) and send that +0.5 s of tail silence — the fixed 1–1.5 s tails stopped endpointing the fixtures after the user raised `min_silence_ms` to 2900 via the settings pane (the first thinking baseline came back with 0 audio frames and two live pipeline tests failed "no reply"; the 404 the first fix hit was appending to the WS *path* — fixed with `urlsplit`+netloc). Config tests de-pinned from shipped defaults: `test_shipped_vivo_toml_is_complete` is now validate-based (every schema key present + `validate(data, voices=(file voice,)) == []` — vivo.toml is user-editable since T019: live file carries `af_nova`/`max_tokens 608`), and `test_config_api.py` asserts against `config.TTS_VOICE` instead of `af_heart`. Full suite **106/106 in 102.48 s**. Bench (repeats 2, reply/long/barge, thinking on; `benchmarks/baseline-thinking-20260916-213401.json` vs `benchmarks/after-clause-20260916-214837.json`; host LLM shared with the session — thinking 2.8–24.6 s, contention-noisy): TTS-side first audio (`llm_first_token` → first `tts_*_sent`) **3.7–8.6 s → 4.5–5.7 s** (mean 6.8 → 5.1 s); first-chunk synth **3.4–6.9 s → 2.5–5.1 s**; server `first_audio` reply **20.8/30.0 → 10.3/8.8 s**, long **16.7/14.0 → 11.2/13.5 s** (LLM thinking dominates the absolute in thinking mode); worst inter-chunk gap **2.6 → 2.0 s** (residue = intrinsic 1×-realtime TTS, 2-deep queue); barge-in unchanged (0.0–0.005 s to ack, 0 stale bytes). Client-observed `first_audio` is filler-masked (~5.7 s both runs) — the server-side `tts_*_sent`-filtered extra is the reply-level metric.
- T024 done 2026-09-18 (D020, wake phrase — revises D013's "wake word = scope creep, new model": implemented on the STT transcript, no new model): new `app/wake.py` — `WakeState` (thread-safe; words-in-order regex matching, punctuation/case tolerant, word-boundary anchored) on `Engines.wake`, shared by all connections (one mic in the room; a wake on any tab wakes vivo for all). `app/pipeline.py`: `_handle_utterance` gate — asleep + no phrase match ⇒ silent drop (no transcript frame, no LLM, nothing stored); phrase-only ⇒ `fixed_reply = WAKE_ACK` (no LLM round trip); phrase+request ⇒ `wake_prefix = WAKE_ACK` spoken first, then the remainder through the normal LLM/TTS loop (transcript keeps the full utterance); awake + end-phrase match ⇒ sleep + spoken `WAKE_GOODNIGHT`; `_wake_tick(session)` from the audio path (every frame) `claim_sleep()`s on the idle timeout — no timer thread — and suppresses expiry while any reply is active (`_any_reply_active()`); `_speak_goodnight()` sends the goodnight as `agent_text` + TTS to every open session so the user knows vivo went quiet. Protocol: `{"type":"wake","active"}` handshake frame (third, after `session` + `config`) + `broadcast_wake()` on every state change; client `{"type":"wake"}` manual-wake command (UI button + live tests). `serve_session` sends it; `config` frame renamed from `barge_config_msg` (now also carries `wake.phrase`). Config: new `[wake]` section — `phrase` ("hey vivo"), `session_timeout_s` (30), `end_phrases` ("that's all", "goodbye", "go to sleep"), `ack` ("Yes?"), `goodnight` ("Okay, going quiet.") — all `apply: now` (schema row + `WAKE_*` env + compose passthrough + `apply_config` → `engines.wake.update()` hot-apply; empty phrase = off = legacy always-answer). Frontend: `asleep` status state (slate LED + dot, shown when mic running + phrase set + not active), hint `sleeping · say "hey vivo" to wake vivo`, wake button (enabled only when on + asleep), asleep `start`/`end` frames suppressed client-side. Tests: new `tests/test_wake.py` (matching incl. punctuation/boundaries + empty off + hot-apply; wake/sleep/`claim_sleep` transitions on a fake clock; pipeline fakes: silent drop, ack-only with **no LLM call**, ack+remainder, end phrase ⇒ goodnight+sleep, idle timeout ⇒ goodnight+sleep, timeout suppressed mid-reply, disabled legacy) + `tests/test_pipeline.py::manual_wake()` for the live tests (shipped `vivo.toml` ships wake-enabled); fakes given disabled `WakeState()` (`test_tts_queue`, `test_thinking_fillers`, `test_sessions`, `test_config_api`). Drive-by: `test_tts_queue.py::test_barge_timestamps_are_cleared_after_generation_finishes` was **already failing on HEAD** (VoiceSession built outside a running event loop; asserted `release()` clears `barge_mono` though it *records* it — the pop happens in the interrupted utterance's `finally`) — rewritten to drive the real `_handle_utterance` finally path inside an event loop. Full suite **172/172** in the app image: 170 offline + the 2 live-WS pipeline tests against a fresh server container (real `qwen3.8-27b` + LuxTTS; fresh `DATA_DIR` needs the voice clip copied in — `data/voices/*.wav` → `/data/voices/`).
- T021 done 2026-09-17 ~10:30 (D017): `app/conversation.py` — new `SessionStore`: many named sessions, one *active*; each session is a `Conversation` persisted at `data/sessions/<id>.json` (existing atomic-write machinery), index `data/sessions.json` = `{active, sessions: {id: {created, last_used, turns}}}` with turn counts kept fresh by a new `Conversation.on_change` callback (fires under the conversation RLock right after `_save()`; the store callback only takes the store lock — no inversion); ids `YYYYMMDD-HHMMSS` + `-2/-3/…` collision suffixes, **retired ids never reused** (`_retired` set — prevents a same-second recreation from rebinding other tabs' `localStorage` pins to empty memory); first start: legacy `data/conversation.json` imported + removed, orphan `sessions/*.json` adopted, corrupt index/session file discarded with a warning. `app/pipeline.py` — `Engines.sessions`; `/ws?session=<id>` pins (unknown → active + warning); `serve_session` sends `{"type":"session","id":…}` as the **first** frame (before `config`) and handles `{"type":"session"}` (create) / `{"type":"session","id":…}` (switch) — both also `set_active`, unknown → `error` frame; `_handle_utterance` captures `conv` at start so an in-flight reply keeps its conversation; `apply_config` → `sessions.set_limits` (`live` badge). `app/main.py` — `GET/POST /api/sessions`, `POST /api/sessions/{id}/activate`, `DELETE /api/sessions/{id}` (404 unknown; last delete creates a fresh active). Frontend — footer session `<select>` (populated from `GET /api/sessions`, `formatSessionId` → "Sep 17 10:24", "(active)" mark) + **new** button; each tab remembers its session in `localStorage` and reconnects with `?session=`; the server's `session` frame is authoritative (stale pins fall back to `j.active`). Test fixtures migrated to real `SessionStore` (`tests/test_tts_queue.py` `make_engines`, `tests/test_thinking_fillers.py` — dropped `FakeConversation`, `tests/test_config_api.py` `_FakeEngines.sessions`); the test `_CmdWS.receive()` fake now `await asyncio.sleep(0)`s and the tests drain after `serve_session` (single-loop: `VoiceSession.send`'s `run_coroutine_threadsafe` tasks only run once the loop yields — true in production because `ws.receive()` awaits the network). 22 new offline tests in `tests/test_sessions.py` → full suite **128/128 in 82.91 s** (live WS tests pass against the rebuilt server — handshake `session` frame first, then `config`). Live: image rebuilt + service `--force-recreate` → first start created `data/sessions.json` + `data/sessions/20260917-101749.json` (one active session, no legacy file to migrate), `/health` OK.
