fastapi>=0.115
uvicorn[standard]>=0.30
faster-whisper>=1.1
httpx>=0.27
ddgs>=9
websockets>=13
python-multipart>=0.0.9
pytest>=8
# LuxTTS (zipvoice) voice-clone TTS (T022). Install CPU-only torch first
# (Dockerfile does this) so pip keeps it instead of the CUDA wheels.
# piper-phonemize comes from the k2-fsa wheel index; linacodec is git-only.
--find-links https://k2-fsa.github.io/icefall/piper_phonemize.html
git+https://github.com/ysharma3501/LinaCodec.git
git+https://github.com/ysharma3501/LuxTTS.git
